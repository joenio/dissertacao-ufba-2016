% my %dataset = %{ $_[0] };
version,total_modules,total_eloc
% foreach my $v (map { $_->{version} } @{ $dataset{releases}{versions} }) {
%   my $total_modules = $dataset{metrics}{"version-$v"}{total_modules};
%   my $total_eloc = $dataset{metrics}{"version-$v"}{total_eloc};
<%== sprintf("%s,%d,%d", $v, $total_modules, $total_eloc) %>
% }
%# vim: filetype=epl
